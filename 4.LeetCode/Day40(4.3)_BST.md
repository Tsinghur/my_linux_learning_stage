# Day40

## 101.[[Easy]108. 将有序数组转换为二叉搜索树](https://leetcode.cn/problems/convert-sorted-array-to-binary-search-tree/description/)——“递归”&“分治”

```c
// 递归&分治
struct TreeNode* arr_to_bst(int* nums, int l, int r){
    if(l > r)
        return NULL;
    int mid = l + (r - l) / 2;
    struct TreeNode* root = (struct TreeNode*)malloc(sizeof(struct TreeNode));
    root->val = nums[mid];
    root->left = arr_to_bst(nums, l, mid - 1);
    root->right = arr_to_bst(nums, mid + 1, r);

    return root;
}

struct TreeNode* sortedArrayToBST(int* nums, int numsSize) {
    return arr_to_bst(nums, 0, numsSize - 1);;
}
```

## 102.[[⭐Medium+]98. 验证二叉搜索树](https://leetcode.cn/problems/validate-binary-search-tree/description/)——“BST”&“递归-前中后序遍历”

```c
// 思路：先中序遍历，再将其副本排序，对比二者是否相同
// 错误：无法检测重复值
// void in_order(struct TreeNode* root, int* a, int* len){
//     struct TreeNode* pre;

//     while(root){
//         pre = root->left;
//         if(pre){
//             while(pre->right && pre->right != root)
//                 pre = pre->right;
//             if(!pre->right){
//                 pre->right = root;
//                 root = root->left;
//                 continue;
//             }
//             if(pre->right == root){
//                 a[(*len)++] = root->val;
//                 pre->right = NULL;
//                 root = root->right;
//             }
//         }else{
//             a[(*len)++] = root->val;
//             root = root->right;
//         }
//     }
// }
// int cmp(const void* a, const void* b){
//     int* elem1 = (int*)a;
//     int* elem2 = (int*)b; 
//     return *elem1 - *elem2;
// }
// bool isValidBST(struct TreeNode* root) {
//     int* tmp1 = (int*)malloc(10000 * sizeof(int));
//     int len = 0;

//     in_order(root, tmp1, &len);
//     int* tmp2 = (int*)malloc(len * sizeof(int));
//     memcpy(tmp2, tmp1, len * sizeof(int));

//     qsort(tmp2, len, sizeof(int), cmp);
//     if(memcmp(tmp1, tmp2, len * sizeof(int)) == 0)
//         return true;
//     return false;
// }

// 法一：前序遍历
// 参数 left 和 right 并不是从根到当前节点路径上的节点值的范围，而是当前节点允许取值的全局约束范围。这个约束会一直向下传递，限制的是整个子树。在根节点，范围是 (LLONG_MIN, LLONG_MAX)
bool dfs(struct TreeNode* node, long long left, long long right){
    if(node == NULL)
        return true;
    long long x = node->val;
    return left < x && x < right && dfs(node->left, left, x) && dfs(node->right, x, right);
}

bool isValidBST(struct TreeNode* root) {
    return dfs(root, LLONG_MIN, LLONG_MAX);
}

//  
```

